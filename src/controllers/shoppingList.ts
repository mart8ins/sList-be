import { RequestHandler } from "express";

export const createShoppingList: RequestHandler = (req, res, next) => {};

export const getShoppingLists: RequestHandler = (req, res, next) => {};

export const deleteShoppingList: RequestHandler = (req, res, next) => {};
