import { RequestHandler } from "express";

export const createRecipe: RequestHandler = (req, res, next) => {};

export const getRecipes: RequestHandler = (req, res, next) => {};

export const deleteRecipe: RequestHandler = (req, res, next) => {};
